<div class="simple-text accesskey <% if class_set %><%= class_set %><% end %>" data-type="<%= $module %>"><div class="in">
	<% if header_text %>
		<div class="hd"><div class="in">
			<h4><span><a href="#"><%= header_text %></a></span></h4>
		</div></div>
	<% end %>
	<div class="ct"><div class="in">
		<span><a href="#" <% if access_key %>accesskey="<%= access_key %>"<% end %> <% if access_title %>title="<%= access_title %>"<% end %>>:::</a></span>
	</div></div>
</div></div>