<%
	min_li = 1 #最少的li數量
	max_li = 10 #最多的li數量
	min_width = 1 #最窄的選單寬度
	max_width = 6 #最寬的選單寬度

	min_navlen = 6
	max_navlen = 10

	li_length = (min_li..max_li).to_a.sample
	nav_width = (min_width..max_width).to_a.sample
	navlen = (min_navlen..max_navlen).to_a.sample
	
	width = rand( li_length )
%>

<div class="group-list nav <% if class_set %><%= class_set %><% end %>" data-setnavlen="<%= navlen %>" data-index="<%= index %>" data-type="<%= $list %>" data-child="<%= li_length %>" <% if func_set %> data-func="<%= func_set %>"<% end %> ><div class="in">
	<% if header_text %>
		<div class="hd"><div class="in">
			<h3><span><a href="#"><%= header_text %></a></span></h3>
		</div></div>
	<% end %>

	<div class="ct"><div class="in"><ul data-index="1" data-child="<%= li_length %>"><% li_length.times do |i| %><li data-index="<%= i + 1 %>" data-width="<%= rand( li_length ) + 1 %>">

					<div class="group nav" data-index="1" data-type="3" data-child="1"><div class="in">
						<div class="hd"><div class="in">
							<h3><span><a href="#"><%= lorem_word %></a></span></h3>
						</div></div>
						<div class="ct"><div class="in">

						<!-- <%= inner %> -->
							<%= render partial:  inner, locals: set( ) %>
						<!-- <%= inner %> -->

						</div></div>
					</div></div>
				
				</li><% end %></ul></div></div>
</div></div>